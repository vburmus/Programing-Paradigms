/*
package figure

import scala.math.*

class Ball(private val r: Double) extends Figure {
  require(r > 0, "Invalid radius for figure.Ball")
  override def volume: Double = (4.0/3.0) * math.Pi * math.pow(r, 3)
}*/
